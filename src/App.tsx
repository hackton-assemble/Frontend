import styled from "styled-components";

const Button = styled.button`
  padding: 24px;
  font-size: 24px;
`

const App = () => {
  return (
    <div>
      <Button>버튼입니다.</Button>
    </div>
  );
};

export default App;
